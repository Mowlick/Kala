import 'package:flutter/material.dart';

class KalaColors {
  // Ivory: Soft warm editorial backdrop
  static const Color ivory = Color(0xFFFAF6F0);
  
  // Charcoal: Heavy editorial text and dark mode base
  static const Color charcoal = Color(0xFF1C1C1C);
  
  // Burnt Orange: Core brand accent used for primary CTA buttons and glowing borders
  static const Color burntOrange = Color(0xFFE35D38);
  
  // Muted Gold: Premium accents and highlight borders
  static const Color mutedGold = Color(0xFFE2BA6E);
  
  // Soft Beige: Pill tag backgrounds and container fills
  static const Color softBeige = Color(0xFFF5EBE0);
  
  // Card Background: Soft warm white elevation block
  static const Color cardBg = Color(0xFFFDFBF9);
  
  // Accent Green: Used for active indicators or completed tags
  static const Color accentGreen = Color(0xFF38B000);
  
  // Light Grey: Neutral borders and labels
  static const Color lightGrey = Color(0xFFE0E0E0);
  
  // Medium Grey: Muted helper text
  static const Color mediumGrey = Color(0xFF7F7F7F);
  
  // Overlay Dark: Transparent black for sheet overlays
  static const Color overlayDark = Color(0x66000000);
}
